# Environment overrides for the Docker Compose `full` profile.
# Usage:
#   docker compose -f infrastructure/docker-compose.yml --env-file infrastructure/full.env --profile full up --build
#
# These URLs point at Compose service DNS names (not localhost).

OPENSEARCH_URL=http://opensearch:9200
PROMETHEUS_URL=http://prometheus:9090
